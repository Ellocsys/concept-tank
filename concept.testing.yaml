phantom:
  address: conceptclub.testing.2nova.com:80 # [Target's address]:[target's port]
  ammo_type: uri
  load_profile:
    load_type: rps # schedule load by defining requests per second
    schedule: line(1, 100, 1m) # starting from 1rps growing linearly to 10rps during 10 minutes
  uris: |
    /
telegraf:
  enabled: true # let's disable telegraf monitoring for this time
